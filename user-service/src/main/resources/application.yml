server:
  port: 9001

spring:
  datasource:
    password:
    url: jdbc:postgresql://localhost:5432/accounts
    username:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

